@page "/"

@inject GameManagerService GameManagerService
@inject IStringLocalizer<Localization> StringLocalizer
@inject MessageBusService MessageBusService

@implements IDisposable

<PageTitle>Numberle</PageTitle>

<div class="flex flex-col justify-between items-center h-full w-full outline-0" @ref="_mainDiv" tabindex="0" @onkeydown="KeyDown">
    <HeaderBar />

    <ToastNotification />

    <GameBoard/>

    @if (_showStats)
    {
        <Modal Title="@StringLocalizer["StatsTitle"]" OnCloseCallback="@CloseStats">
            <StatsDialog />
        </Modal>
    }
</div>